import React from 'react'
import './donateWindow.scss'

type Props = {}

export default function DonateWindow (props: Props) {
  return <div id={'donate-window'}>
    this is a donation window
  </div>
}
